"""Arborist — an MCP server for Scanopy.

Arborist tends the canopy view: it reads everything Scanopy discovered and
curates only the user-owned overlay (names, descriptions, visibility, tags,
bindings). It never mutates the discovered layer.
"""

__version__ = "0.1.0"
